{
  "name": "powerdown",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && npm run dev",
    "dev": "start cmd /c \"cd server && npm run dev\" && cd client && npm run dev",
    "install:all": "npm install --prefix client && npm install --prefix server",
    "build": "npm install --prefix client --include=dev && npm install --prefix server --include=dev && npm run build --prefix client && npm run build --prefix server",
    "start": "npm run start --prefix server",
    "db:init": "npm run db:init --prefix server"
  }
}
